import { WorldOption } from './world.js';

export const RenderOption = {
  PADDING_LEFT: 10 * WorldOption.MULTIPLEXER,
  PADDING_TOP: 8 * WorldOption.MULTIPLEXER,
  TILE_SIZE: WorldOption.TILE_SIZE,
  UNIT_SIZE: WorldOption.UNIT_SIZE,
  MULTIPLEXER: WorldOption.MULTIPLEXER,
};
